apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-nodeport
spec:
  type: NodePort
  selector:
    app: scaletestapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001